name: Legacy Android Debug APK (manual notice only)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  deprecated:
    runs-on: ubuntu-latest
    steps:
      - name: Legacy workflow disabled
        run: |
          echo "This legacy workflow no longer builds an APK."
          echo "Use: Crypto TradeStation v4 Canonical Update APK"
          echo "That workflow applies the cumulative v4 migration before compiling and is the only automatic APK build."
